# ========================================
# NETWORK PRINTING CONFIGURATION
# ========================================
# Copy these to your .env.local file

# Thermal Printer Network Configuration
# --------------------------------------
# IP address of your 80mm thermal POS printer
# This should be accessible via WireGuard VPN from Digital Ocean
# Find printer IP: Check printer settings or router DHCP table
PRINTER_IP_ADDRESS=192.168.100.160

# Printer port (9100 is standard for thermal printers using RAW TCP)
PRINTER_PORT=9100

# Print Processor Configuration
# --------------------------------------
# Secret token for authenticating print worker requests
# Generate with: openssl rand -hex 32
# IMPORTANT: Keep this secret! Don't commit to version control
PRINT_PROCESSOR_SECRET=change_this_to_a_random_secret_string

# How often to check for new print jobs (in milliseconds)
# 15000 = 15 seconds (recommended)
# Lower values = faster printing but more database queries
# Higher values = slower printing but less load
PRINT_PROCESS_INTERVAL=15000

# ========================================
# EXAMPLE CONFIGURATIONS
# ========================================

# Development (local printer on same network)
# PRINTER_IP_ADDRESS=192.168.1.100
# PRINTER_PORT=9100
# PRINT_PROCESS_INTERVAL=10000

# Production (printer via VPN)
# PRINTER_IP_ADDRESS=192.168.100.160
# PRINTER_PORT=9100
# PRINT_PROCESS_INTERVAL=15000

# Multiple Printers (requires code modification)
# KITCHEN_PRINTER_IP=192.168.100.50
# BAR_PRINTER_IP=192.168.100.51
# PACKING_PRINTER_IP=192.168.100.52

# ========================================
# TESTING
# ========================================
# To test your printer configuration:
#   1. Copy these settings to .env.local
#   2. Run: node scripts/test-printer.js
#   3. Verify test receipt prints
#
# To test connectivity only:
#   ping 192.168.100.160
#   nc -zv 192.168.100.160 9100
